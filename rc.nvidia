#! /bin/bash

if [ -h /etc/X11/xinit/xinitrc.d/intel ]; then
    until [ $(ps -A | grep -c "[tty,l,s].*[g,t,k,d]dm") -ge 1 ]; do
	sleep 1
    done
    sleep 10
    /usr/sbin/nvidia-prime-select nvidia
fi

exit 0
